using UnityEngine;

public class IntroSceneDialogue : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public Dialog dialog;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
